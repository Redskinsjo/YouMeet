query GetEmployees($filter: String, $sort: Int) {
  employees(filter: $filter, sort: $sort) {
    _id
    fullname
    firstname
    lastname
    email
    avatar
    color
    from
    lat
    long
    starting
    job
    description
    timestamp
  }
}
