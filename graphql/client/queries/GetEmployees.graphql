query GetEmployees($filter: String, $sort: Int) {
  employees(filter: $filter, sort: $sort) {
    id
    fullname
    firstname
    lastname
    email
    avatar
    color
    from
    lat
    long
    starting
    job
    description
  }
}
